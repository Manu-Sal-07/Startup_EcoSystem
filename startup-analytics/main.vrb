\frametitle{System Verification – Pytest Infrastructure}
\begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{scard}{Mock Testing Configurations}
        \begin{itemize}
          \item Simulates graph drivers to test route logics in memory.
          \item Restricts external queries to ensure rapid execution speeds.
          \item Mocks JWT payloads to bypass login stages during endpoint checks.
        \end{itemize}
      \end{scard}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{scard}{Boundary Testing Cases}
        \begin{itemize}
          \item Validates transaction locks under simulated parallel conditions.
          \item Asserts that unauthorized role access yields 403 HTTP codes.
          \item Tests limit compliance when wallet values edge close to zero.
        \end{itemize}
      \end{scard}
    \end{column}
  \end{columns}
